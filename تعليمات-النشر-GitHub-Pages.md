# تعليمات النشر — قدراتي

## قبل الرفع

احذفي محتويات النسخة السابقة من المستودع أو استبدليها بهذه النسخة. لا تتركي Workflow قديمًا داخل `.github/workflows/` بجانب `pages.yml`.

## خطوات النشر

1. ارفعي **جميع محتويات هذه النسخة إلى جذر المستودع** على الفرع `main`.
2. من **Settings → Pages** اختاري **GitHub Actions** كمصدر النشر.
3. افتحي **Actions → Deploy GitHub Pages**.
4. انتظري نجاح وظيفتي `build` و`deploy` ثم افتحي رابط الموقع.

## الإصلاحات الفعلية في هذه النسخة

- إزالة استدعاءات Supabase من الكود التنفيذي نفسه، وليس حذف المكتبة فقط.
- إزالة رفع الملفات وقاعدة البيانات والخدمات الخلفية من النسخة العامة.
- إعادة إصدارات Next/React/Lucide إلى الإصدارات الأصلية التي بُني عليها المشروع بدل ترقيتها بلا حاجة.
- عدم السماح لأي Action بإعادة كتابة `next.config.mjs`.
- حساب `BASE_PATH` تلقائيًا من اسم المستودع، ثم بناء Next.js بهذا المسار.
- الاكتفاء بـ `basePath`؛ لأن Next.js يضبط مسار أصول `_next` معه، دون `assetPrefix` إضافي غير لازم.
- استخدام إجراءات GitHub Actions المبنية على Node 24 للنشر.
- إنشاء Artifact Pages بصيغة `artifact.tar` المطلوبة ثم رفعه باسم `github-pages`.
- التحقق من وجود `out/index.html` و`out/_next` قبل النشر.

## ملفات PDF

ضعي ملفات PDF داخل `public/files/` حسب الأسماء في `README-PDF.txt`.
